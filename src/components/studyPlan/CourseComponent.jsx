import React from "react";

const CourseComponent = ({ title }) => {
  return (
    <div>
      CourseComponent
      <h2 className="bg-pink-500">{title}</h2>
    </div>
  );
};

export default CourseComponent;
